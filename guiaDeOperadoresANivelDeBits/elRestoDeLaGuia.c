#include <stdio.h>
#define BYTE 8

void ponerACero (){

}

void cargarBits (unsigned char* puntero){
    printf("Se cargarán 1 bytes en binarios.\n----------------------------\n");
    
    printf("Ingrese una serie de bits: ");

    for (int i = 0; i < BYTE; ++i){

    }
}
int main (void) {

    unsigned char bits[BYTE] = {0};
    unsigned char* pBits = bits;

    cargarBits(pBits);

    return 0;
}

